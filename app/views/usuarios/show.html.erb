<html>
	<body>
		<h3> Perfil de <%= @usuario.nome %> </h3>
		<%= @usuario.email %>
		<% if current_user == @usuario %>
			<ul>
				<li><%= link_to "Edit", edit_usuario_path(current_user) %></li>
				<li><%= link_to "Delete", @usuario, method: :delete%></li>
			</ul>
		<% end %>
	</body>
</html>